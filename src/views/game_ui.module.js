const game_ui = {
    /* 
        @description: A function to render the game board UI based on game state.
        @param {array}: A matrix representing the game state.

        @return {undefined}

    */
    renderGameBoard(gameState) {

    }
};

module.exports = {
    renderGameBoard: game_ui.renderGameBoard.bind(game_ui),
};